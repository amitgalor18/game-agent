# Voice-Controlled Game Map — Python 3.10+
# LLM (LM Studio)
openai>=1.0.0

# ASR
faster-whisper>=1.0.0

# Audio capture (VAD)
SpeechRecognition>=3.10.0
pyaudio>=0.2.14

# Terminal colors
colorama>=0.4.6

# Optional: numpy for audio handling (often pulled in by faster-whisper)
numpy>=1.24.0

# Web API (for python main.py --api)
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.0.0
pydub>=0.25.0
