# Same as requirements-offline.txt but WITHOUT pyaudio.
# Use this when PortAudio dev headers are not installed (Linux: portaudio19-dev).
# The web demo (run_all.sh) works without PyAudio; only the desktop PTT UI (main.py) needs it.

wheel
setuptools
scikit-build-core>=0.9.2
pyproject-metadata
cmake>=3.21
ninja>=1.5

openai>=1.0.0
faster-whisper>=1.0.0
SpeechRecognition>=3.10.0
colorama>=0.4.6
numpy>=1.24.0
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.0.0
pydub>=0.25.0
python-multipart>=0.0.6

llama-cpp-python[server]>=0.2.0
